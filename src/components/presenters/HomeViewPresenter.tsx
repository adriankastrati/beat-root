import { useState } from "react"

// implement at a later stage a nice landing page with highlighted beats etc
const HomeViewPresenter = () => {
    const [isLoggedIn, setIsLoggedIn] = useState(false)
    

    return (
        <div>
            uh 
        </div>
    )
}

export default HomeViewPresenter
