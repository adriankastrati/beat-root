import styled from "styled-components";
import { theme } from "../../../common";

const DeleteCrossButton = styled.button`
    background-color: ${theme.dark};
    width:20px;
    height:20px;
`

export default DeleteCrossButton;